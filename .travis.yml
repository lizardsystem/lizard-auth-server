language: python
sudo: false
python:
  - 3.5
addons:
  apt:
    packages:
     - python3-psycopg2
cache:
  directories:
   - eggs
   - $HOME/.local
install:
  - deactivate
  - /usr/bin/python3 bootstrap.py
  - bin/buildout -v
script:
  - bin/test
after_success:
  - bin/coveralls
