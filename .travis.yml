language: python
sudo: false
python:
  - 2.7
addons:
  apt:
    packages:
     - python-psycopg2
cache:
  directories:
   - eggs
   - $HOME/.local
install:
  - deactivate
  - /usr/bin/python2.7 bootstrap.py
  - bin/buildout -v
script:
  - bin/test
after_success:
  - bin/coveralls
